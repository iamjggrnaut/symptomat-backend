{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "purrweb",
  "password": "purrweb",
  "database": "surveys",
  "entities": ["src/**/*.entity.ts"],
  "migrations": ["migration/*.ts"],
  "seeds": ["seeds/**/*.seed.ts"],
  "factories": ["factories/**/*.factory.ts"],
  "cli": {
    "migrationsDir": "migration"
  }
}
